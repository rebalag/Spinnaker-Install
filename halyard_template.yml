apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: spin-halyard
  namespace: spinnaker 
  labels:
    app: spin
    stack: halyard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spin
      stack: halyard
  template:
    metadata:
      labels:
        app: spin
        stack: halyard
    spec:
      containers:
      - name: halyard-daemon
        # todo - make :stable or digest of :stable
        image: opsmx11/halyard:stable
        imagePullPolicy: Always
        command:
        - /bin/sh
        args:
        - -c
        - "cp -R /home/builder/staging/.hal /home/builder/.hal;sh /opt/run_halyard.sh"
        readinessProbe:
          exec:
            command:
            - wget
            - -q
            - --spider
            - http://localhost:8064/health
        ports:
        - containerPort: 8064
        volumeMounts:
        - name: halconfig
          mountPath: /home/builder/staging/.hal/config
          subPath: config
        - name: kubeconfig
          mountPath: /home/builder/staging/.kube/config
          subPath: config
        - name: externalipconfig
          mountPath: /home/builder/staging/.hal/default/service-settings/deck.yml
          subPath: deck.yml
        - name: externalipconfig
          mountPath: /home/builder/staging/.hal/default/service-settings/gate.yml
          subPath: gate.yml
        - name: artifactsconfig
          mountPath: /home/builder/staging/.hal/default/service-settings/redis.yml
          subPath: redis.yml 
        - name: artifactsconfig
          mountPath: /home/builder/staging/.hal/default/profiles/front50-local.yml
          subPath: front50-local.yml
        - name: artifactsconfig
          mountPath: /home/builder/staging/.hal/default/service-settings/front50.yml
          subPath: front50.yml
        - name: artifactsconfig
          mountPath: /home/builder/staging/.hal/default/service-settings/deck.yml
          subPath: deck.yml
        - name: artifactsconfig
          mountPath: /home/builder/staging/.hal/default/service-settings/fiat.yml
          subPath: fiat.yml
      volumes:
      - name: halconfig
        configMap:
          defaultMode: 420
          name: halconfig
      - name: kubeconfig
        secret:
          defaultMode: 420
          secretName: kubeconfig
      - name: externalipconfig
        configMap: 
          defaultMode: 420
          name: externalipconfig
      - name: artifactsconfig
        configMap: 
          defaultMode: 420
          name: artifactsconfig

---

apiVersion: v1
kind: Service
metadata:
  name: spin-halyard
  namespace: spinnaker 
spec:
  ports:
    - port: 8064
      targetPort: 8064
      protocol: TCP
  selector:
    app: spin
    stack: halyard
  type: ClusterIP
  
---

apiVersion: v1
kind: ConfigMap
metadata:
  namespace: spinnaker
  name: externalipconfig
data:
  gate.yml: |
    host: 0.0.0.0
  deck.yml: |
    host: 0.0.0.0
---
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: spinnaker
  name: artifactsconfig
data: 
  redis.yml: |
    artifactId: opsmx11/redis:v3
  front50-local.yml: |
    spinnaker.s3.versioning: false
  front50.yml: |
    artifactId: docker.io/opsmx11/front50:0.14.0-20181207134351
  deck.yml: |
    artifactId: docker.io/opsmx11/deck:2.6.0-20181213100452
  fiat.yml: |
    artifactId: docker.io/opsmx11/fiat:1.3.0-20181207134351

